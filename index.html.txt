<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>C&amp;C's Custom Remodeling LLC</title>

  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&display=swap" rel="stylesheet">

  <style>
    :root{
      --bg:#ffffff;
      --text:#111111;
      --muted:#666;
      --accent:#000;
      --max-width:1100px;
      --radius:14px;
      --gap:1.25rem;
    }
    *{box-sizing:border-box}
    html,body{height:100%}
    body{
      margin:0;
      font-family:Inter,system-ui,-apple-system,"Segoe UI",Roboto,"Helvetica Neue",Arial;
      color:var(--text);
      background:var(--bg);
      -webkit-font-smoothing:antialiased;
      -moz-osx-font-smoothing:grayscale;
      line-height:1.45;
    }

    .container{max-width:var(--max-width);margin:0 auto;padding:2rem;}
    header{display:flex;align-items:center;justify-content:space-between;gap:1rem;padding:1rem 0;}
    .brand{display:flex;align-items:center;gap:.75rem;text-decoration:none;color:inherit;}
    .logo{width:64px;height:64px;display:inline-grid;place-items:center;border-radius:8px;background:#f6f6f6;overflow:hidden;flex-shrink:0;box-shadow:0 2px 6px rgba(0,0,0,0.06);}
    .logo img{width:100%;height:100%;object-fit:contain;display:block}
    .brand h1{font-size:1.1rem;margin:0;font-weight:700;letter-spacing:0.4px}
    .brand p{margin:0;font-size:.78rem;color:var(--muted);font-weight:500}
    nav{display:flex;gap:1rem}
    nav a{color:var(--muted);text-decoration:none;font-weight:600;font-size:.95rem}
    .cta{padding:.55rem .9rem;border-radius:8px;border:1px solid #111;background:transparent;font-weight:700;}

    .hero{display:grid;grid-template-columns:1fr 420px;gap:2rem;align-items:center;margin:2rem 0;}
    .hero-text h2{margin:0;font-size:2.1rem;line-height:1.05}
    .hero-text p{color:var(--muted);margin-top:.6rem;font-size:1rem}
    .quick-contact{margin-top:1.2rem;display:flex;gap:.75rem;flex-wrap:wrap}
    .phone-pill{background:#111;color:#fff;padding:.65rem .85rem;border-radius:10px;font-weight:700;text-decoration:none}

    .services{display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:1rem;margin:2rem 0;}
    .card{border-radius:12px;padding:1rem;border:1px solid #eaeaea;background:linear-gradient(180deg,#fff,#fafafa);box-shadow:0 6px 18px rgba(0,0,0,0.03);}
    .card h3{margin:.2rem 0;font-size:1.05rem}
    .card p{margin:0;color:var(--muted);font-size:.95rem}

    .gallery{display:grid;grid-template-columns:repeat(3,1fr);gap:.6rem;margin:1rem 0 2rem 0;}
    .gallery img{width:100%;height:160px;object-fit:cover;border-radius:10px}

    .two-col{display:grid;grid-template-columns:1fr 380px;gap:1.25rem;align-items:start}
    .about p{color:var(--muted)}
    .contact{padding:1rem;border-radius:12px;border:1px solid #eaeaea;background:linear-gradient(180deg,#fff,#fafafa);}
    .form-row{display:flex;gap:.5rem}
    .form-row input{flex:1;padding:.6rem .8rem;border-radius:8px;border:1px solid #ddd}
    textarea{width:100%;min-height:110px;padding:.75rem;border-radius:8px;border:1px solid #ddd;resize:vertical}
    button{display:inline-block;padding:.6rem .9rem;border-radius:9px;border:0;background:#111;color:#fff;font-weight:700;cursor:pointer}

    footer{margin:2.5rem 0;color:var(--muted);font-size:.95rem;text-align:center;padding-bottom:2rem}
    @media (max-width:880px){.hero{grid-template-columns:1fr;text-align:center}.two-col{grid-template-columns:1fr}.gallery{grid-template-columns:repeat(2,1fr)}.logo{width:56px;height:56px}nav{display:none}}
  </style>
</head>
<body>
  <div class="container">
    <header>
      <a class="brand" href="#top">
        <div class="logo">
          <img src="images/logo.png" alt="C&amp;C's Custom Remodeling logo" onerror="this.style.display='none'">
        </div>
        <div>
          <h1>C&amp;C's Custom Remodeling LLC</h1>
          <p>Remodel • Renovate • Restore</p>
        </div>
      </a>
      <nav>
        <a href="#services">Services</a>
        <a href="#gallery">Gallery</a>
        <a href="#about">About</a>
        <a href="#contact" class="cta">Contact</a>
      </nav>
    </header>

    <main>
      <section class="hero" id="top">
        <div class="hero-text">
          <h2>Quali
